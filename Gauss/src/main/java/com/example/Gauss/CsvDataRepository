package com.example.Gauss;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CsvDataRepository extends JpaRepository<CsvData, Long> {
    JpaRepository<CsvData>, Long> {
    }